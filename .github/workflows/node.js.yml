name: Build

on:
  push:
    branches: local

jobs:
  build:
    name: build
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 14.x
      - run: node ./build.js
      - run: git add build.js
      - run: git commit
